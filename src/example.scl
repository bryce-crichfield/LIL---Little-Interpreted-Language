program
    define a as 1
    define b as 2
    procedure one does
        while(a < b) do
            display(10)
        end
    end
    if(a < b) then
        run one
        display(a)
    end
end