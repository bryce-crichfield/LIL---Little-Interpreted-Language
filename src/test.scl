program
    define n as 10
    define result as 0

    procedure fibonacci does
            define b as 1
            define c as 1
            define i as 1
            while i < n do
                set result to b
                set b to c
                set c to result + b
                set i to i + 1
            end
    end
    run fibonacci
    display(b)
end

